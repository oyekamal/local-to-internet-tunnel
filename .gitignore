

#Ignore vscode AI rules
.github/instructions/codacy.instructions.md

# Ignore downloaded binaries but keep the bin/ folder structure
bin/cloudflared*
bin/*.exe
bin/*.zip
bin/*.tar.gz

# Keep the bin directory in git but ignore its contents
!bin/
!bin/.gitkeep